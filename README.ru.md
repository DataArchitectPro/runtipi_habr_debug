# Runtipi v4.6.5 (Proxmox LXC): проверка подключения к внешнему PostgreSQL

> 🇬🇧 [Read this README in English](README.md)

## Контекст
- Proxmox → LXC-контейнер → внутри Docker/Compose
- Runtipi: `v4.6.5`, каталог `/opt/runtipi`
- Контейнеры Runtipi: `runtipi`, `runtipi-reverse-proxy`, `runtipi-queue`, `runtipi-db (postgres:14)`
- Цель: перевести Runtipi на внешний PostgreSQL (другой LXC в той же сети) и отказаться от `runtipi-db`.

Внешний Postgres: `192.168.100.106:5432`, БД `tipi`, пользователь `tipi`.
Пароль (хеш) в файлах: `de99…377b` *(не секрет, но на всякий случай в тексте замаскировано)*.

---

## Что в архиве

Архив содержит 2 “снимка” системы:

### `00_Исходное_состояние/` — до изменений
**`00_Исходное_состояние/конфиги/`**
- `.env` — исходный env Runtipi. Ключевое: `POSTGRES_HOST=runtipi-db`
- `docker-compose.yml` — исходный compose-файл (с сервисом `runtipi-db`)

**`00_Исходное_состояние/docker/`**
- `inspect_runtipi.json` — результат `docker inspect runtipi`
- `inspect_runtipi-db.json` — результат `docker inspect runtipi-db`
- `docker-compose.config.yml` — результат `docker compose config` (итоговая конфигурация глазами compose)

**`00_Исходное_состояние/логи/`**
- `runtipi.log` — `docker logs runtipi`
- `runtipi-db.log` — `docker logs runtipi-db`

---

### `01_Попытка_внешний_PostgreSQL/` — после попытки переключения
**`01_Попытка_внешний_PostgreSQL/конфиги/`**
- `.env.local` — попытка задать внешний Postgres:
  - `POSTGRES_HOST=192.168.100.106`
  - `POSTGRES_PORT=5432`
  - `POSTGRES_DBNAME=tipi`
  - `POSTGRES_USERNAME=tipi`
  - `POSTGRES_PASSWORD=de99…377b`
- `.env` — при этом **остался с `POSTGRES_HOST=runtipi-db`**
- `docker-compose.yml` — всё ещё содержит сервис `runtipi-db` (postgres:14)

**`01_Попытка_внешний_PostgreSQL/docker/`**
- `inspect_runtipi.json` — `docker inspect runtipi` после изменений
- `docker-compose.config.yml` — `docker compose config` после изменений (**важно для вывода**)

**`01_Попытка_внешний_PostgreSQL/логи/`**
- `runtipi.log` — `docker logs runtipi`
- `runtipi-db.log` — `docker logs runtipi-db`

**`01_Попытка_внешний_PostgreSQL/вывод_команд/`**
- `env_inside_container.txt` — вывод `env` внутри контейнера `runtipi`
- `runtipi-cli_status.txt` — попытка `./runtipi-cli status` (в этом запуске “command not found”)

**`01_Попытка_внешний_PostgreSQL/доп_проверки_после_изменений.txt`**
- список контейнеров `docker ps ...` и прочие проверки после изменений

---

## Что получилось по фактам из файлов (ключевые наблюдения)

### 1) Внутри контейнера `runtipi` переменные указывают на внешний Postgres
Файл: `01_.../вывод_команд/env_inside_container.txt`

Там есть:
- `POSTGRES_HOST=192.168.100.106`
- `POSTGRES_PORT=5432`
- `POSTGRES_DBNAME=tipi`
- `POSTGRES_USERNAME=tipi`
- `POSTGRES_PASSWORD=...`

И это же подтверждается:
Файл: `01_.../docker/inspect_runtipi.json` → `Config.Env` содержит `POSTGRES_HOST=192.168.100.106`.

➡️ То есть “в контейнер” параметры внешней БД реально попали.

---

### 2) Но Docker Compose “видит” другое: `POSTGRES_HOST: runtipi-db` и `depends_on: runtipi-db`
Файл: `01_.../docker/docker-compose.config.yml`

В нём явно:
- `depends_on: runtipi-db`
- `POSTGRES_HOST: runtipi-db`

➡️ Это означает: **docker compose config строится не из `.env.local`, а из обычного `.env`** (или из сгенерённого окружения), и продолжает считать, что Postgres — локальный сервис.

---

### 3) Сервис `runtipi-db` в реальности продолжает подниматься
Файл: `01_.../доп_проверки_после_изменений.txt`

Там в `docker ps`:
- `runtipi-db postgres:14 ... Up ... (healthy)`

Плюс в `01_.../конфиги/docker-compose.yml` сервис `runtipi-db:` присутствует.

➡️ Даже после попыток “вынести БД наружу”, локальный postgres контейнер остаётся частью стека и запускается.

---

### 4) Ключевая улика: контейнер `runtipi` **монтирует** `/opt/runtipi/.env` внутрь себя как `/data/.env`
Файл: `01_.../docker/inspect_runtipi.json` → `Mounts`

Там есть bind-mount:
- `Source: /opt/runtipi/.env`
- `Destination: /data/.env`

А файл `/opt/runtipi/.env` (в снимке `01_.../конфиги/.env`) содержит:
- `POSTGRES_HOST=runtipi-db`

В логах `runtipi` видно строку:
- `Generating system env file`

➡️ Очень похоже, что Runtipi на старте **генерирует/перезаписывает “system env” на основе `/data/.env`**, то есть на основе **реального `/opt/runtipi/.env`**, а не `.env.local`.

---

## Итоговый вывод по этому архиву
По собранным данным выходит, что одновременно выполняются два факта:
1) в контейнер `runtipi` действительно попали `POSTGRES_*` на внешний хост;
2) но сам Runtipi/compose-стек остаётся привязан к локальному `runtipi-db`, потому что:
   - compose-итог (`docker compose config`) продолжает содержать `POSTGRES_HOST=runtipi-db` и `depends_on: runtipi-db`;
   - `runtipi` монтирует `/opt/runtipi/.env` как `/data/.env`, и этот файл всё ещё указывает на `runtipi-db`.

То есть “переключение через `.env.local`” выглядит **недостаточным/обходным**: требуется либо менять `/opt/runtipi/.env` (который реально используется), либо менять логику запуска/генерации env в Runtipi, либо полностью убирать сервис `runtipi-db` из compose и проверять, не пересоздаёт ли его CLI/генератор.

---

## Вопрос к знатокам Runtipi
1) Какой “каноничный” способ задать внешний PostgreSQL так, чтобы Runtipi **не генерировал env обратно на `runtipi-db`**?
2) Где именно хранится “истина” для DB-конфига: переменные контейнера, `/data/.env`, какой-то `system.env`, `state`, `user-config`?
3) Если использовать `--env-file`, то на какой стадии он применяется (CLI vs docker compose), и почему `docker compose config` остаётся с `runtipi-db`?

---

## Файлы с самой полезной информацией
- `01_.../docker/inspect_runtipi.json` — Mounts + Env внутри контейнера
- `01_.../docker/docker-compose.config.yml` — что реально думает compose (там `POSTGRES_HOST=runtipi-db`)
- `01_.../вывод_команд/env_inside_container.txt` — env внутри контейнера (там внешний IP)
- `01_.../доп_проверки_после_изменений.txt` — `docker ps`, видно что `runtipi-db` живой
- `01_.../конфиги/.env` и `.env.local` — конфликтующие источники истины
